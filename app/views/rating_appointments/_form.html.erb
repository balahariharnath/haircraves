<%= form_with(model: rating_appointment) do |form| %>
  <% if rating_appointment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rating_appointment.errors.count, "error") %> prohibited this rating_appointment from being saved:</h2>

      <ul>
        <% rating_appointment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :rate, style: "display: block" %>
    <%= form.text_field :rate %>
  </div>

  <div>
    <%= form.label :appointment_id, style: "display: block" %>
    <%= form.text_field :appointment_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
